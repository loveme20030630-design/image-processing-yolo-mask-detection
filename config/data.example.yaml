# Copy this file to data.yaml and change path to your local dataset directory.
path: /absolute/path/to/yolo_dataset
train: images/train
val: images/val
test: images/test

names:
  0: without_mask
  1: with_mask

